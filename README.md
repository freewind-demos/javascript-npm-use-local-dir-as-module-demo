JavaScript Npm Demo
===================

Make sure npm version >= 5:

```
npm --version
```

Note: lower version has problems which can't handle relative path 'file:../~common'.

```
npm install
npm run demo
```

You will see:

```
Hello, Npm!
```

